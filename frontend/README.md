# BlackLanches Frontend (minimal)

Pequeno scaffold React usando Vite.

Instalação e execução:

```bash
cd frontend
npm install
npm run dev
```

Abra `http://localhost:5173` (Vite) e certifique-se de que o backend está rodando em `http://localhost:3000`.
