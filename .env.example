# ==========================================
# 🔐 VARIÁVEIS DE AMBIENTE - BLACKLANCHES
# ==========================================
# Este é um arquivo de EXEMPLO das variáveis de ambiente necessárias
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha com seus valores reais
# 3. NUNCA comite o arquivo .env (ele está no .gitignore)
#
# Por que isso é importante?
# - Mantém informações sensíveis fora do código
# - Permite configurações diferentes por ambiente (dev, test, prod)
# - Segurança: senhas não ficam expostas no repositório

# ==========================================
# 🗄️ BANCO DE DADOS
# ==========================================
# URL de conexão com o PostgreSQL
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO
#
# Exemplos:
# - Local: postgresql://postgres:1234@localhost:5432/blacklanches
# - Neon: postgresql://user:password@ep-xxx.neon.tech/blacklanches
# - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

DATABASE_URL=postgresql://postgres:password@localhost:5432/blacklanches

# ==========================================
# 🔑 AUTENTICAÇÃO JWT
# ==========================================
# Chave secreta usada para assinar tokens JWT
# 
# IMPORTANTE:
# - Use uma string longa e aleatória
# - NUNCA compartilhe esta chave
# - Mude em produção
#
# Gerar uma chave segura:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=sua-chave-secreta-super-segura-aqui

# ==========================================
# 🌐 SERVIDOR
# ==========================================
# Porta onde o servidor vai rodar
# Padrão: 3000

PORT=3000

# Ambiente de execução
# Valores: development, production, test
NODE_ENV=development

# ==========================================
# 📝 NOTAS PARA PRODUÇÃO
# ==========================================
#
# Ao fazer deploy em serviços como:
# - Heroku
# - Railway
# - Render
# - Vercel
# - AWS
#
# Configure estas variáveis no painel do serviço,
# NÃO no arquivo .env (que não deve ser commitado).
#
# Exemplo no Heroku:
# heroku config:set DATABASE_URL=postgresql://...
# heroku config:set JWT_SECRET=...
#
